src/components/ui/*

# Wayfinder output — sinh tự động từ backend (composer wayfinder); để generator
# giữ format riêng, tránh Prettier reformat rồi bị regenerate ghi đè (đồng bộ
# với eslint ignore).
src/actions
src/routes
src/wayfinder
